# 快速啟動指南

這份指南將幫助你在 5 分鐘內啟動並運行這個作品集網站。

## 前置需求

- Node.js 18+ 已安裝
- 終端機或命令提示字元

## 步驟 1: 安裝相依套件

```bash
cd backend
npm install
```

## 步驟 2: 初始化資料庫

```bash
npm run init-db
```

這會建立 SQLite 資料庫並建立預設管理員帳號：
- 使用者名稱: `admin`
- 密碼: `admin123`

## 步驟 3: 插入範例資料（可選）

```bash
npm run seed
```

這會插入 3 個範例專案和 5 個範例開發紀錄。

## 步驟 4: 啟動伺服器

```bash
npm start
```

伺服器將在 `http://localhost:3000` 啟動。

## 步驟 5: 訪問網站

開啟瀏覽器並訪問：

- **前台網站**: http://localhost:3000/
- **後台管理**: http://localhost:3000/admin/login.html

## 後台登入

使用預設帳號登入後台：
- 使用者名稱: `admin`
- 密碼: `admin123`

⚠️ **重要**: 首次登入後請立即修改密碼！

## 開始使用

### 新增專案

1. 登入後台
2. 點擊「專案管理」
3. 點擊「新增專案」
4. 填寫專案資訊
5. 使用 Markdown 編輯器撰寫內容
6. 上傳專案截圖
7. 點擊「儲存」

### 新增開發紀錄

1. 登入後台
2. 點擊「開發紀錄」
3. 點擊「新增開發紀錄」
4. 填寫標題、分類、標籤
5. 使用 Markdown 編輯器撰寫內容
6. 點擊「儲存」

### 查看前台

1. 訪問 http://localhost:3000/
2. 瀏覽首頁、關於我、專案作品、開發紀錄
3. 點擊專案卡片查看詳情
4. 為喜歡的內容點讚

## 開發模式

如果你想要在開發時自動重啟伺服器：

```bash
npm run dev
```

這會使用 nodemon 監控檔案變更並自動重啟。

## 執行測試

```bash
# API 單元測試
npm test

# 整合測試
npm run test:integration
```

## 常見問題

### Q: 無法連接到伺服器
A: 確認伺服器已啟動，並檢查 3000 埠是否被佔用。

### Q: 登入失敗
A: 確認使用正確的帳號密碼（admin/admin123），並確認資料庫已初始化。

### Q: 圖片上傳失敗
A: 確認 backend/uploads 目錄存在且有寫入權限。

### Q: 前台無法載入資料
A: 確認後端伺服器正在運行，並檢查瀏覽器控制台的錯誤訊息。

## 下一步

- 閱讀 [README.md](README.md) 了解完整功能
- 查看 [TESTING.md](TESTING.md) 了解測試方法
- 自訂配色和樣式（修改 frontend/css/main.css）
- 修改個人資訊（about.html）
- 部署到生產環境

## 需要幫助？

如果遇到問題，請檢查：
1. Node.js 版本是否正確（18+）
2. 所有相依套件是否已安裝
3. 資料庫是否已初始化
4. 伺服器是否正在運行
5. 瀏覽器控制台的錯誤訊息

## 停止伺服器

在終端機按 `Ctrl + C` 停止伺服器。

---

祝你使用愉快！🎉
